import DataFormatter from './DataFormatter';

class EarthDataFormatter extends DataFormatter {
  formatData(rawData) {
    // Formatação
      
    return {
      // Retorno do JSON padronizado
    };
  }
}

export default EarthDataFormatter;
